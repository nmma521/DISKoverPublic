import React from "react";
import "./Playlist.css"

const Playlist = () => {
    return (
        <div className="Playlist">
            <button>Apply Filter</button>
        </div>
    );
};

export default Playlist;